import React from 'react';
import ReactDOM from 'react-dom';





import Game from './Game.js';

// Render the App component inside the root element of the HTML file
ReactDOM.render(
  <React.StrictMode>


    <Game />
   
  </React.StrictMode>,
  document.getElementById('root')
);
